import Head from 'next/head';
import { Navbar } from '../components';

const Home: React.FC = () => (
  <>
    <Head>
      <title>Rodrigo Luz App</title>
    </Head>
    <Navbar/>
  </>
);

export default Home;
